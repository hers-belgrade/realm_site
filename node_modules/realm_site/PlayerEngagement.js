function PlayerEngagement(player,bcaster){
  this.path = ['nodes',bcaster.serverName,'rooms',bcaster.roomName];
};


PlayerEngagement.produce = function(player,bcaster){
  var pdata = player.mychannel.bcaster.data;
  var eel = pdata.element(['engagements']);

  if(!eel){
  }
};
